const 
    { WebhookClient, MessageEmbed } = require("discord.js"),
    tokenLogin = require("../../configs/tokens.js")


module.exports = async (shardID, err) => {
    const webhook = new WebhookClient()
}